

<%= form_for([@post, @post.comments.build]) do |f| %>

    <div class="field">
      <%= f.hidden_field :commenter ,value: current_user.username %>
      <%= f.label :body %><br />
      <%= f.text_area :body %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>